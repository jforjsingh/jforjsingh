#include<stdio.h>
int main()
{
	int n,c,arm=0,r;
	printf ("enter a numbers:");
	scanf("%d",&n);
	c=n;
	while(n>0)
	{
		r=n%10;
		arm=arm+(r*r*r);
		n=n/10;
	}
	if (c==arm)
	printf("the number is armstrong ");
	else 
	printf("the number is not armstrong ");
	return 0;
}   
